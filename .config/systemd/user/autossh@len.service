[Unit]
Description=len tunnel
After=network.target

[Service]
ExecStart=/usr/bin/ssh -o ServerAliveInterval=60 -o ExitOnForwardFailure=yes -N -R 7070:localhost:22 xk@pakon
Restart=always
RestartSec=15

[Install]
WantedBy=default.target
