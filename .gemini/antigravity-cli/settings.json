{
  "model": "Gemini 3.1 Pro (Low)",
  "permissions": {
    "allow": [
      "command(cargo test)",
      "command(./gradlew)",
      "command(find)",
      "command(head)",
      "command(cargo)",
      "command(go)",
      "command(mv)",
      "command(sort)",
      "command(mkdir)",
      "command(git show)",
      "command(git log)",
      "command(grep)",
      "command(echo)",
      "command(git ls-tree)",
      "command(ls)",
      "command(sed)",
      "command(cat)",
      "command(protoc)",
      "command(git clone)",
      "command(rustup)",
      "command(wc)",
      "command(git ls-remote)",
      "command(rg)",
      "command(curl -s https://docs\\.rs/)",
      "command(make)",
      "command(git status)"
    ]
  }
}
